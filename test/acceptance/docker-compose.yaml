services:
  wdio-cucumber:
    build: .
    image: wdio-cucumber
    depends_on:
      - selenium
    volumes:
      - ./reports/:/home/node/reports/
    ports:
      - "9228:9229"

  selenium:
    image: selenium/standalone-chrome
    container_name: chrome-browser
    volumes:
      - /dev/shm:/dev/shm
